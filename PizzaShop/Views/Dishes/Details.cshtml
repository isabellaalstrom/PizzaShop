@using PizzaShop.Entities
@model PizzaShop.Entities.Dish

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Dish</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DishName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DishName)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Price)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Price)
        </dd>
    </dl>
</div>
<div>
    <dl class="dl-horizontal">
        <dt>Ingredients</dt>
        @foreach (var ingredient in Model.DishIngredients)
        {
            <dd>@ingredient.Ingredient.IngredientName</dd>
        }
    </dl>
</div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model.DishId">Edit</a> |
        <a asp-action="Index">Back to List</a>
    </div>

