@using PizzaShop.Infrastructure
@model Cart
<div class="navbar-right">
    @if (Model.Items.Any())
    {
        <small class="navbar-text">
            <a class="navbar-link" asp-controller="Cart" asp-action="Index">
                <b>Your cart:</b>
                @Model.Items.Count() item(s)
                @Model.ComputeTotalValue().ToString("c")
            </a>
        </small>
    }
    <a class="btn btn-sm btn-default navbar-btn"
       asp-controller="Cart" asp-action="Index"
       asp-route-returnurl="@ViewContext.HttpContext.Request.PathAndQuery()">
        <i class="fa fa-shopping-cart"></i>
    </a>
</div>

@*@model CartIndexViewModel

    @if (Model.Cart..Count > 0)
    {
        <li>
            <a>
                <span class="i fa fa-lg fa-shopping-cart"></span>
                <span id="cart-status">
                    @Model.Cart._cartItems.Count
                </span>
            </a>
        </li>
    }*@